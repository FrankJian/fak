; 大纲查询：INI（SPEC F6.1，含 `.cfg` `.conf` `.properties`）。
;
; 段落节点包住它名下的所有设置项，缩进因此自动成立；段落之外的顶层设置
; 仍会作为 depth 0 的条目列出。

(section
  (section_name) @name) @definition.module

(setting
  (setting_name) @name) @definition.key
